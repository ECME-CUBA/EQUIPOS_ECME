version: '2'
services:
  equipos-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/equipos/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=equipos
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
